import { PrintableDirective } from './printable.directive';

describe('PrintableDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintableDirective();
    expect(directive).toBeTruthy();
  });
});
